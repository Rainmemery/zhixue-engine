# 智学引擎 - 系统架构文档

> 本文档帮助你理解智学引擎的整体设计，知道各个模块是怎么协作的，以及你应该在哪里添加代码。

---

## 整体架构

智学引擎是一个**前后端分离**的**微服务**系统。简单来说：

- **前端**负责展示界面，用户在这里写代码、和 AI 聊天
- **后端**有很多个独立的服务，各自负责不同的功能
- **网关**是所有请求的入口，负责把请求转发到正确的服务

```
┌─────────────────────────────────────────────────────────────────────┐
│                        用户浏览器                                    │
│         登录 | 刷题 | AI对话 | 管理后台 | 知识库                      │
└─────────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    🚪 API 网关 (端口 8080)                           │
│                                                                     │
│    所有请求都从这里进入 → 转发到对应的后端服务                         │
│    负责：路由 | 限流 | 跨域 | 服务发现                                │
└─────────────────────────────────────────────────────────────────────┘
                                │
            ┌───────────────────┼───────────────────┐
            ▼                   ▼                   ▼
    ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
    │ 👤 用户服务  │   │ 📝 题目服务  │   │ 📚 学习服务  │
    │   端口 8081  │   │   端口 8082  │   │   端口 8083  │
    │              │   │              │   │              │
    │ 登录注册     │   │ 题目管理     │   │ 学习记录     │
    │ 用户信息     │   │ 代码评测     │   │ 学习路径     │
    └──────────────┘   └──────────────┘   └──────────────┘
            │                   │                   │
            └───────────────────┼───────────────────┘
                                │
            ┌───────────────────┼───────────────────┐
            ▼                   ▼                   ▼
    ┌──────────────┐   ┌──────────────┐   ┌──────────────┐
    │ 🤖 AI 服务   │   │ 🔧 管理服务  │   │ 📦 公共模块  │
    │   端口 8086  │   │   端口 8090  │   │  (不独立运行) │
    │              │   │              │   │              │
    │ AI对话       │   │ 用户管理     │   │ 工具类       │
    │ RAG知识库    │   │ 系统监控     │   │ JWT工具      │
    └──────────────┘   └──────────────┘   └──────────────┘
            │
            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                        💾 数据存储层                                 │
│                                                                     │
│    ┌──────────┐   ┌──────────┐   ┌──────────┐   ┌──────────┐      │
│    │  MySQL   │   │  Redis   │   │  Milvus  │   │  Ollama  │      │
│    │  业务数据 │   │  缓存    │   │  向量数据 │   │  AI模型  │      │
│    └──────────┘   └──────────┘   └──────────┘   └──────────┘      │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 各模块详解

### 📦 zhixue-common（公共模块）

**作用**：其他所有模块都依赖它，提供通用工具。

```
zhixue-common/
├── Result.java          # 统一 API 响应格式
├── JwtUtils.java        # JWT 令牌生成与验证
└── ...
```

**你需要知道的**：
- 不会独立运行，只是被其他模块引用
- 如果要添加公共工具类，放这里

---

### 🚪 zhixue-getaway（API 网关）

**作用**：所有请求的入口，负责路由转发。

```
用户请求 → 网关 → 判断路径 → 转发到对应服务

/api/v1/auth/**      → zhixue-user    (登录注册)
/api/v1/problems/**  → zhixue-problem (题目)
/api/v1/learning/**  → zhixue-learning(学习)
/api/v1/ai/**        → zhixue-ai      (AI功能)
/api/v1/admin/**     → zhixue-admin   (管理后台)
/ws/**               → zhixue-problem (WebSocket)
```

**你需要知道的**：
- 配置文件：`src/main/resources/application.yaml`
- 新增服务？在这里添加路由规则
- 路由规则在 `spring.cloud.gateway.routes` 下

---

### 👤 zhixue-user（用户服务）

**作用**：处理用户相关的所有事情。

| 功能 | 接口 | 说明 |
|------|------|------|
| 注册 | POST /api/v1/auth/register | 创建新账号 |
| 登录 | POST /api/v1/auth/login | 获取令牌 |
| 刷新令牌 | POST /api/v1/auth/refresh | 续期 |
| 用户信息 | GET /api/v1/users/{id} | 获取用户资料 |
| 修改资料 | PUT /api/v1/users/{id} | 更新信息 |

**你需要知道的**：
- 端口：8081
- 数据库：zhixue_user
- Controller：`controller/` 目录
- Service：`service/` 目录
- Mapper：`mapper/` 目录

---

### 📝 zhixue-problem（题目服务）

**作用**：管理题目、接收代码提交、执行评测。

```
用户提交代码 → 题目服务 → Docker 容器执行 → 返回结果
                      ↕
                 WebSocket 实时推送
```

**你需要知道的**：
- 端口：8082
- 数据库：zhixue_problem
- 评测在 Docker 容器中执行（安全隔离）
- 支持 WebSocket 实时推送评测结果

---

### 📚 zhixue-learning（学习服务）

**作用**：记录学习行为，生成学习路径。

| 功能 | 说明 |
|------|------|
| 学习记录 | 记录用户做了什么题、得了多少分 |
| 学习路径 | 根据用户水平生成个性化学习计划 |
| 学习统计 | 分析学习数据，给出建议 |

**你需要知道的**：
- 端口：8083
- 数据库：zhixue_learning

---

### 🤖 zhixue-ai（AI 服务）

**作用**：所有 AI 相关功能都在这里。

| 功能 | 说明 | 接口 |
|------|------|------|
| 代码解释 | AI 分析代码逻辑 | POST /api/v1/ai/explain |
| 代码评审 | AI 检查代码质量 | POST /api/v1/ai/review |
| 协作者 | AI 协助优化代码 | POST /api/v1/ai/collaborate |
| 智能提问 | AI 生成问题帮你复习 | POST /api/v1/ai/questioner |
| RAG 知识库 | 上传文档，AI 基于文档回答 | POST /api/v1/rag/** |

**你需要知道的**：
- 端口：8086
- 数据库：zhixue_ai
- 使用 SSE 流式响应（实时输出 AI 生成的内容）
- 依赖 Ollama（AI模型）和 Milvus（向量数据库）

---

### 🔧 zhixue-admin（管理服务）

**作用**：后台管理系统，管理员用的。

| 功能 | 说明 |
|------|------|
| 用户管理 | 查看、编辑、删除用户 |
| 题目审核 | 审核新提交的题目 |
| 系统监控 | 查看服务器 CPU、内存、磁盘 |
| 数据备份 | 备份和恢复数据库 |

**你需要知道的**：
- 端口：8090
- 数据库：多数据源（admin/user/problem/learning）

---

## 数据库设计

### 数据库列表

| 数据库 | 服务 | 存什么 |
|--------|------|--------|
| zhixue_user | user | 用户信息、学习偏好 |
| zhixue_problem | problem | 题目、分类、代码提交记录 |
| zhixue_learning | learning | 学习记录、学习路径 |
| zhixue_ai | ai | 知识库、文档、向量数据 |
| zhixue_admin | admin | 管理员信息、操作日志 |

### 核心表

**用户相关**
- `users` — 用户基础信息（用户名、密码、邮箱...）
- `user_learning_profiles` — 学习偏好（编程语言、难度偏好...）

**题目相关**
- `problems` — 题目信息（标题、描述、测试用例...）
- `problem_categories` — 题目分类
- `code_submissions` — 代码提交记录

**学习相关**
- `learning_records` — 学习记录
- `learning_paths` — 学习路径
- `learning_path_steps` — 路径中的每一步

**AI 相关**
- `rag_knowledge_bases` — 知识库
- `rag_documents` — 上传的文档
- `rag_chunks` — 文档分块（用于检索）

---

## 认证机制

系统使用 **JWT（JSON Web Token）** 进行认证，采用双令牌机制：

```
用户登录
   ↓
返回 Access Token（24小时有效）+ Refresh Token（7天有效）
   ↓
后续请求携带：Authorization: Bearer <access_token>
   ↓
Access Token 过期？用 Refresh Token 换新的
   ↓
Refresh Token 也过期？重新登录
```

**Token 有效期**：
- Access Token：24 小时（短期，安全）
- Refresh Token：7 天（长期，方便）

---

## API 接口规范

### 请求格式

```
基础 URL: http://gateway:8080/api/v1

请求头:
  Content-Type: application/json
  Authorization: Bearer <token>
```

### 响应格式

```json
{
    "code": 200,
    "message": "success",
    "data": { ... },
    "timestamp": 1735228800000
}
```

### 接口总览

| 模块 | 路径前缀 | 功能 |
|------|----------|------|
| 认证 | /api/v1/auth/ | 登录、注册、刷新 |
| 用户 | /api/v1/users/ | 用户信息管理 |
| 题目 | /api/v1/problems/ | 题目查询 |
| 提交 | /api/v1/submissions/ | 代码提交评测 |
| 学习 | /api/v1/learning/ | 学习记录 |
| AI | /api/v1/ai/ | AI 对话功能 |
| RAG | /api/v1/rag/ | 知识库管理 |
| 管理 | /api/v1/admin/ | 后台管理 |

---

## 服务间通信

```
┌──────────┐    REST API    ┌──────────┐
│ 服务 A   │ ───────────→  │ 服务 B   │
└──────────┘                └──────────┘
       │                         ↑
       │    Nacos 服务发现        │
       └─────────────────────────┘

┌──────────┐   WebSocket   ┌──────────┐
│  题目服务 │ ←──────────→ │  前端    │
└──────────┘  实时推送结果  └──────────┘
```

- **同步调用**：服务之间通过 REST API 通信
- **服务发现**：通过 Nacos 注册和发现服务
- **消息推送**：题目评测结果通过 WebSocket 实时推送

---

## 你应该在哪里改代码？

### 场景 1：新增一个 API 接口

```
1. 在对应服务的 controller/ 目录添加方法
2. 在 service/ 目录添加业务逻辑
3. 在 mapper/ 目录添加数据库操作（如果需要）
4. 在 resources/mapper/ 目录添加 SQL（MyBatis）
5. 重启服务测试
```

### 场景 2：新增一个页面

```
1. 在 frontend/app/ 目录添加路由
2. 在 frontend/components/ 添加组件
3. 在 frontend/services/ 添加 API 调用
4. 在 frontend/types/ 添加类型定义
```

### 场景 3：新增一个服务

```
1. 创建新模块目录 zhixue-xxx/
2. 添加 pom.xml（参考其他模块）
3. 创建主类 ZhixueXxxApplication.java
4. 在根 pom.xml 的 <modules> 中添加
5. 在 zhixue-getaway 的路由中添加规则
6. 在 docker/ 中添加 Dockerfile
7. 在 docker-compose.yml 中添加服务
8. 创建数据库和表
```

### 场景 4：修改数据库

```
1. 在 docker/init-scripts/tables/ 中修改对应的 SQL
2. 或者手动连接 MySQL 执行 ALTER 语句
3. 更新对应的 Mapper XML
```

---

## 配置管理

### 环境变量

所有服务支持通过环境变量覆盖 `application.yaml` 中的配置：

```bash
MYSQL_PASSWORD=你的密码
JWT_SECRET=你的密钥
NACOS_IP=127.0.0.1
```

### Nacos 配置中心

所有服务注册到 Nacos，实现：
- 服务发现（A 服务可以找到 B 服务）
- 动态配置（不用重启就能改配置）

---

## 安全设计

| 安全措施 | 说明 |
|----------|------|
| 密码加密 | 使用 SHA-256 哈希存储，不存明文 |
| JWT 认证 | 双令牌机制，安全且方便 |
| 接口限流 | 防止恶意请求，保护服务器 |
| CORS 配置 | 网关统一管理跨域 |
| Docker 隔离 | 代码评测在容器中执行，安全隔离 |

---

## 技术术语表

| 术语 | 含义 |
|------|------|
| JWT | JSON Web Token，一种身份认证方式 |
| SSE | Server-Sent Events，服务器推送事件（用于 AI 流式输出） |
| RAG | Retrieval-Augmented Generation，检索增强生成（让 AI 基于你的文档回答） |
| Nacos | 阿里开源的服务注册发现和配置中心 |
| Milvus | 开源向量数据库（用于存储文档的向量表示） |
| Ollama | 本地运行 AI 模型的工具 |
| MyBatis | Java 的 ORM 框架（帮你操作数据库） |
| WebSocket | 全双工通信协议（用于实时推送） |

---

## 还想了解更多？

- 看 [README.md](README.md) 了解项目概况
- 看 [DEPLOY.md](DEPLOY.md) 搭建开发环境
- 直接看代码！从 Controller 开始，跟着调用链走

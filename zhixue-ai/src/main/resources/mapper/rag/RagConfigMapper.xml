<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rain.zhixueai.mapper.rag.RagConfigMapper">
    
    <resultMap id="RagConfigResultMap" type="com.rain.zhixueai.entity.rag.RagConfig">
        <id property="id" column="id"/>
        <result property="configType" column="config_type"/>
        <result property="configKey" column="config_key"/>
        <result property="configValue" column="config_value"/>
        <result property="description" column="description"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>
    
    <select id="findAll" resultMap="RagConfigResultMap">
        SELECT * FROM rag_config ORDER BY config_type, config_key
    </select>
    
    <select id="findByType" resultMap="RagConfigResultMap">
        SELECT * FROM rag_config WHERE config_type = #{configType} ORDER BY config_key
    </select>
    
    <select id="findByKey" resultMap="RagConfigResultMap">
        SELECT * FROM rag_config WHERE config_type = #{configType} AND config_key = #{configKey}
    </select>
    
    <insert id="insert" parameterType="com.rain.zhixueai.entity.rag.RagConfig" 
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO rag_config (
            config_type, config_key, config_value, description, 
            created_at, updated_at
        ) VALUES (
            #{configType}, #{configKey}, #{configValue}, #{description},
            NOW(), NOW()
        )
    </insert>
    
    <update id="update" parameterType="com.rain.zhixueai.entity.rag.RagConfig">
        UPDATE rag_config SET
            config_value = #{configValue},
            description = #{description},
            updated_at = NOW()
        WHERE id = #{id}
    </update>
    
    <delete id="delete">
        DELETE FROM rag_config WHERE id = #{id}
    </delete>
    
    <delete id="deleteByKey">
        DELETE FROM rag_config WHERE config_type = #{configType} AND config_key = #{configKey}
    </delete>
    
    <insert id="upsert" parameterType="com.rain.zhixueai.entity.rag.RagConfig">
        INSERT INTO rag_config (
            config_type, config_key, config_value, description, 
            created_at, updated_at
        ) VALUES (
            #{configType}, #{configKey}, #{configValue}, #{description},
            NOW(), NOW()
        )
        ON DUPLICATE KEY UPDATE
            config_value = #{configValue},
            description = #{description},
            updated_at = NOW()
    </insert>
</mapper>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.rain.zhixueuser.mapper.AuthMapper">
    <insert id="insertUser" parameterType="com.rain.zhixueuser.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users(
                         username,
                         email,
                         password_hash,
                         phone,
                         real_name
        )
        VALUES (
                #{username},
                #{email},
                #{passwordHash},
                #{phone},
                #{realName}
               )
    </insert>
    <select id="selectByUsername" resultType="com.rain.zhixueuser.entity.User">
        SELECT id,username,password_hash,email,learning_level,experience_points
        FROM users
        WHERE username=#{username}
    </select>
    <insert id="insertUserProfile">
        INSERT INTO user_profiles (user_id, programming_language, difficulty_preference, daily_goal_minutes, learning_style, weekly_learning_days)
        VALUES (#{userId}, 'python', 'adaptive', 60, 'visual', 5)
    </insert>
    <insert id="initUserAchievements">
        INSERT INTO user_achievements (user_id, achievement_id, progress_current, progress_target)
        VALUES
        <foreach collection="achievementIds" item="achievementId" separator=",">
            (#{userId}, #{achievementId}, 0, 1)
        </foreach>
    </insert>
    <select id="selectActiveAchievementIds" resultType="java.lang.Integer">
        SELECT id FROM achievements WHERE is_active = 1
    </select>
</mapper>
